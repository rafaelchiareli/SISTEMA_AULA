@model IEnumerable<SISTEMA_AULA.MODEL.ViewModel.EntradaVM>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CodigoEntrada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DataEntarda)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroNF)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CodigoEntrada)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataEntarda)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NumeroNF)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Create", new { id = item.CodigoEntrada }) |
                    @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                    @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
                </td>
            </tr>
        }
    </tbody>
</table>
